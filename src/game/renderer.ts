// Placeholder renderer — will be replaced in Task 15 (Main Entry + Integration)
import { GameState } from "./types";
import { GameEngine } from "./engine";

export class Renderer {
  update(_state: GameState, _engine: GameEngine): void {
    // No-op stub
  }
}
